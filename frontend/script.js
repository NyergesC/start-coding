function getDateOfHighestDeposit (account) {
    const dateOfHighest = NULL;
    const amountOfHighest = 0;

    for (const transactions of account) {

        if (typeof transactions === "deposit" && transactions === isSuccessful) {
            console.log()
        }
        
    }


}